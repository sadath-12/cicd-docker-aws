import { atom } from "recoil";

export const sellerState = atom({
    key: "SELLER_STATE_RECOIL_STATE",
    default: '',
})
export const relatedState = atom({
    key: "RELATED_STATE_RECOIL_STATE",
    default: '',
})
